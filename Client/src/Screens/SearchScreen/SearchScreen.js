import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import SearchScreencomp from '../../Components/SearchScreencomponents/SearchScreencomp'

const SearchScreen = () => {
    return (
        <View style={{ flex: 1 }}>
            <SearchScreencomp />
        </View>
    )
}

export default SearchScreen

const styles = StyleSheet.create({})