import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import MusicScreenComp from '../Components/MusicScreenComp'

const MusicScreen = () => {
    return (
        <View style={{ flex: 1 }}>
            <MusicScreenComp />
        </View>
    )
}

export default MusicScreen

const styles = StyleSheet.create({})